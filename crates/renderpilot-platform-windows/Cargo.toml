[package]
name = "renderpilot-platform-windows"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Windows platform adapter boundary for RenderPilot."

[lints]
workspace = true

[dependencies]
renderpilot-application = { path = "../renderpilot-application" }
renderpilot-detection = { path = "../renderpilot-detection" }
renderpilot-domain = { path = "../renderpilot-domain" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.11"
quick-xml = "0.42"

[target.'cfg(windows)'.dependencies]
winreg = "0.56"
windows = { version = "0.62.2", features = ["ApplicationModel", "Management_Deployment", "Storage_Search"] }

[dev-dependencies]
tempfile = "3"

[lib]
path = "src/lib.rs"
