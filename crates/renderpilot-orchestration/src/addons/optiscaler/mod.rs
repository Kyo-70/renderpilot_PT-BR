//! Narrow OptiScaler configuration projection used by RenoDX.
//!
//! The complete OptiScaler lifecycle is introduced in C3.  C2 deliberately
//! exposes only the byte-preserving ReShade-chain reducer required by the
//! RenoDX-owned coordinated route.

pub(crate) mod config;
